#ifndef CLIENT_SIMULATION_TICKER_H
#define CLIENT_SIMULATION_TICKER_H

#include "Simulation/CommandList.h"

namespace Client {
  namespace SimulationTicker {
    void tick(Simulation::CommandList &commands);
  }
}

#endif
