#ifndef RENDERING_BACKEND_TEXTURE_HANDLE_H
#define RENDERING_BACKEND_TEXTURE_HANDLE_H

#include <stdint.h>

namespace Rendering {
  namespace Backend {
    typedef uint8_t TextureHandle;
  }
}

#endif
